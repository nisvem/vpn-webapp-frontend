import { ComponentType } from "react";
declare type TelegramWebAppProps = {
    children: JSX.Element;
    validateHash(hash: string): boolean | Promise<boolean>;
};
export declare function TelegramWebApp({ children, validateHash }: TelegramWebAppProps): JSX.Element;
export declare function withTelegramWebApp(Component: ComponentType<any>, contextProps: Omit<TelegramWebAppProps, "children">): (props: any) => JSX.Element;
export {};
